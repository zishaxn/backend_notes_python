# ✅ What is PIP
Pip is python package installer. Its used to donwlaod the packages from internet.


- When you install any package without virtual env its installed globally.



 - To see list of packages installed use below command in terminal.

    ```bash 
    pip list
    ```